# 🚀 Как загрузить бота и веб-приложение

## 📋 Что можно загрузить на GitHub Pages:

### ✅ На GitHub Pages (https://kreemchek.github.io/regerg24/):

Эти файлы будут работать сразу:

1. **index.html** - главная страница
2. **styles.css** - стили
3. **script.js** - логика приложения
4. **commission.xlsx** - база товаров

**GitHub Pages = статический хостинг**, работает сразу после загрузки файлов.

---

## ❌ Что НЕ работает на GitHub Pages:

### Python бот (bot.py)

**GitHub Pages НЕ поддерживает Python!**

Для `bot.py` нужен отдельный сервер с Python:
- Heroku (бесплатно)
- Railway (бесплатно)
- PythonAnywhere (бесплатно)
- Render (бесплатно)
- VPS сервер (платно)

---

## 🔧 Как это работает вместе:

### Вариант 1: Все на GitHub (только веб-приложение)

1. Загрузите на GitHub Pages:
   - `index.html`
   - `styles.css`
   - `script.js`
   - `commission.xlsx`

2. **Без бота** - веб-приложение будет работать напрямую в браузере!

**✅ Плюсы:** Просто, бесплатно, работает сразу
**❌ Минусы:** Нет Telegram бота

---

### Вариант 2: Веб-приложение + Бот (рекомендуется)

#### Шаг 1: Загрузите веб-приложение на GitHub Pages

```bash
# На GitHub Pages
- index.html
- styles.css
- script.js
- commission.xlsx
```

**URL:** `https://kreemchek.github.io/regerg24/`

#### Шаг 2: Загрузите бота на отдельный сервер

**На Heroku (бесплатно):**

1. Создайте аккаунт на https://heroku.com
2. Установите Heroku CLI
3. Создайте приложение:
```bash
heroku create your-bot-name
heroku git:remote -a your-bot-name
git push heroku main
```

4. Установите переменные окружения в Heroku:
```bash
heroku config:set BOT_TOKEN=ваш_токен
heroku config:set WEB_APP_URL=https://kreemchek.github.io/regerg24/
heroku config:set ADMIN_CHAT_ID=ваш_chat_id
```

5. Бот заработает автоматически!

**На Railway (бесплатно, проще):**

1. Зайдите на https://railway.app
2. New Project → Deploy from GitHub
3. Выберите репозиторий
4. Railway автоматически найдет `bot.py` и запустит
5. Добавьте переменные окружения в настройках

---

## 📦 Структура проекта для GitHub:

### Для GitHub Pages (репозиторий regerg24):

```
regerg24/
├── index.html          ✅
├── styles.css          ✅
├── script.js           ✅
├── commission.xlsx    ✅
└── README.md           ✅
```

### Для бота (отдельный репозиторий или та же папка):

```
telegram-bot/
├── bot.py              ✅ (для Heroku/Railway)
├── requirements.txt    ✅
└── Procfile            ✅ (для Heroku)
```

---

## 🎯 Быстрый старт:

### 1. Веб-приложение на GitHub:

```bash
cd "/Users/zalogudachi/Downloads/Архив/WebUnit-economik"
git add index.html styles.css script.js commission.xlsx
git commit -m "Web app ready"
git push
```

### 2. Бот на Heroku (самый простой способ):

1. Создайте файл `Procfile` (для Heroku):
```
worker: python bot.py
```

2. На Heroku:
```bash
heroku create
git push heroku main
heroku config:set BOT_TOKEN=...
heroku config:set WEB_APP_URL=https://kreemchek.github.io/regerg24/
```

---

## ✅ Итого:

**GitHub Pages** → Веб-приложение работает сразу  
**Heroku/Railway** → Бот работает отдельно

Они работают **независимо**, но бот может открывать веб-приложение через `WEB_APP_URL`.

---

## 💡 Рекомендация:

1. **Сначала** загрузите веб-приложение на GitHub Pages
2. **Проверьте**, что оно работает
3. **Потом** добавьте бота на Heroku/Railway

Веб-приложение может работать **БЕЗ бота** - просто откройте `index.html` в браузере!

